/* Having a seperate utils and index.js files is redundant
 * for a project of this size but is nevertheless a good
 * practice to cater for when more files become neeeded
 * and makes this more easily searchable through the file name
 */

const utils = require('./utils')

module.exports = utils
